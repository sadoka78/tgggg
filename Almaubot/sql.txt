CREATE DATABASE university_bot;

USE university_bot;

CREATE TABLE teachers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    fullname VARCHAR(255) NOT NULL,
    role ENUM('учитель') NOT NULL,
    majors VARCHAR(255) NOT NULL
);

CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    fullname VARCHAR(255) NOT NULL,
    major VARCHAR(255) NOT NULL
);

ALTER TABLE teachers
MODIFY majors VARCHAR(500);

ALTER TABLE teachers ADD COLUMN code VARCHAR(255);

UPDATE teachers 
SET code = 'BSA212' 
WHERE id = 1;

DELETE FROM teachers 
WHERE id = 3;





INSERT INTO teachers (fullname, role, majors, code) 
VALUES ('Круглова Ирина Сергеевна', 'учитель', 'Software engineering', 'BSA212');

SELECT  FROM teachers WHERE fullname = Круглова Ирина Сергеевна

SELECT  FROM university_bot.students
SELECT  FROM university_bot.teachers


DROP DATABASE university_bot;
